apiVersion: v1
kind: Service
metadata:
  labels:
    k8s-app: kubernetes-dashboard
  name:  dashboard-loadbalancer
  namespace: kubernetes-dashboard
spec:
  selector:
    k8s-app: kubernetes-dashboard
  type:  LoadBalancer
  ports:
  - port:  443
    targetPort: 8443  # not sure why it is 8443
